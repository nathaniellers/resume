import logo from '../images/logo192.png';

const Navbar = () => {
    return (
        <nav className="bg-dark p-2 text-light">
            <img src={logo} alt={logo} height="50"/> React Over
        </nav>
    )
}

export default Navbar
